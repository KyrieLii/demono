import { Configuration } from "webpack";

const config: Configuration = {
  entry: {},
  output: {},
  module: {
    rules: [
      {
        use: "",
        resolve: {},
      },
    ],
  },
  devtool: "",
};

export default config;
